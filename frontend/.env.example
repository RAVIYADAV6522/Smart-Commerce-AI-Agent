# Copy to .env — Vite reads this at dev/build time.

# Local backend
VITE_API_URL=http://localhost:3000

# Vercel (same project, multi-service): use your deployment URL + /backend, no trailing slash
# VITE_API_URL=https://your-project.vercel.app/backend
